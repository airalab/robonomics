{
  "name": "robonomics-cps-indexer",
  "version": "1.0.0",
  "description": "SubQuery indexer for Robonomics CPS (Cyber-Physical Systems) Pallet",
  "main": "dist/index.js",
  "scripts": {
    "build": "subql build",
    "codegen": "subql codegen",
    "start:docker": "docker-compose pull && docker-compose up --remove-orphans",
    "dev": "subql codegen && subql build && docker-compose pull && docker-compose up --remove-orphans",
    "prepack": "rm -rf dist && npm run build",
    "test": "jest --detectOpenHandles",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "homepage": "https://github.com/airalab/robonomics",
  "repository": {
    "type": "git",
    "url": "https://github.com/airalab/robonomics.git",
    "directory": "subquery-cps-indexer"
  },
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Robonomics Network",
  "license": "Apache-2.0",
  "devDependencies": {
    "@subql/cli": "^5.0.0",
    "@subql/testing": "^2.0.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@subql/common": "^3.0.0",
    "@subql/types": "^3.0.0",
    "@subql/types-substrate": "^3.0.0"
  },
  "resolutions": {
    "ipfs-unixfs": "6.0.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
